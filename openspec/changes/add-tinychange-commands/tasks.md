## 1. External schema repo (prerequisite, outside home-manager)

- [x] 1.1 Created `speclib/openspec-tinychange-schema` with `openspec/schemas/tinychange/schema.yaml`
      (generated by `openspec schema init --artifacts specs,tasks`, validated) + `templates/`; pushed,
      public, raw URLs return 200
- [x] 1.2 `README.md` — self-documenting (what/when, `skip_specs` escape, provenance → mipnix archive)
- [x] 1.3 `AGENT_INSTALL.md` — agent-executable: skip-if-present, git-clone or raw-download the schema
      into the project, `openspec schema validate tinychange`. Verified by simulating the install into
      a fresh project (validates + creates a specs+tasks change)

## 2. cc-commands

- [x] 2.1 Added `"mip:tinychange-explore"` to `_cc-commands.nix`: schema presence check → install via
      the live AGENT_INSTALL.md if absent → brief explore + full spec scan → `openspec new change
      --schema tinychange` → delta (or `skip_specs: true`) + tasks.md
- [x] 2.2 Added `"mip:tinychange-apply"`: implement via the schema-aware apply flow → `openspec archive`
      → commit as Pim Snel, no self-promotion, no push
- [x] 2.3 Explore command references the public raw AGENT_INSTALL.md URL and the repo source

## 3. Verification

- [x] 3.1 `nix-instantiate --parse` OK; `nix eval` confirms both `mip:tinychange-explore` and
      `mip:tinychange-apply` keys materialize. (Schema install path independently proven live: raw URLs
      200, and the install-into-fresh-project simulation validated + created a specs+tasks change)
- [ ] 3.2 Post-rebuild: in a project WITHOUT the schema, `/mip:tinychange-explore` installs `tinychange`,
      validates it, and creates a specs+tasks change (no proposal/design) — NEEDS REBUILD + running the command
- [ ] 3.3 Post-rebuild: in a project WITH the schema, explore skips install and proceeds — NEEDS REBUILD
- [ ] 3.4 Post-rebuild: `/mip:tinychange-apply` implements, archives (delta synced), commits with no
      self-promoting trailer — NEEDS REBUILD
- [ ] 3.5 Post-rebuild: a no-spec-impact path sets `skip_specs: true` and still validates/archives
      (mechanics already proven in the spike; needs confirming via the command) — NEEDS REBUILD
