## 1. Pure helpers

- [x] 1.1 Add `packages/mipbar/widget/displays.ts` — no GTK/AGS imports, in the style of
      `widget/monitors.ts`. Define a `DisplayInfo` type and parse `hyprctl monitors all -j`
      into it (name, description, make/model, width/height, refreshRate, x/y, scale,
      transform, focused, dpmsStatus, vrr, disabled, mirrorOf, activeWorkspace,
      physicalWidth/Height, availableModes).
- [x] 1.2 `parseModes(availableModes)`: parse `"1920x1080@60.05Hz"` strings, group by
      `WxH`, keep the highest rate per resolution, sort descending by pixel count.
- [x] 1.3 `diagonalInches(physicalWidth, physicalHeight)` and `density(w, h, diagMm, scale)`
      returning native and effective DPI.
- [x] 1.4 `snapScale(width, height, scale)`: return `null` when `width % scale === 0` and
      `height % scale === 0`, otherwise the scale Hyprland will substitute. Verify against
      the spike observation that `1280x1024` at scale `1.5` becomes `1.6`.
- [x] 1.5 `logicalSize(width, height, scale)` for the `scale 1.50 → 1280×720` line.

## 2. Displays widget

- [x] 2.1 Add `packages/mipbar/widget/Displays.tsx` as a `menubutton` + `<popover>`,
      following the `SystemMonitor.tsx` idiom. Hold parsed `DisplayInfo[]` in
      `createState`.
- [x] 2.2 Read `hyprctl monitors all -j` when the popover opens. Do not poll, and do not
      read through AstalHyprland — it never learns about mode changes (see design.md).
- [x] 2.3 Render one card per monitor: device illustration/photo and accent via the
      existing `monitors.ts` helpers (`monitorAccent`, `deviceType`, `portLabel`,
      `softFill`), then description, diagonal, resolution + refresh, scale → logical size,
      native/effective DPI, position, connector, port chip, active workspace.
- [x] 2.4 Mark the focused monitor. Show rotation only when `transform !== 0`, and
      mirroring only when `mirrorOf !== "none"`.
- [x] 2.5 Render the resolution rows from `parseModes`, marking the current row and the
      native row.
- [x] 2.6 Annotate rows where `snapScale` is non-null with the resulting scale.
- [x] 2.7 On row click, dispatch
      `hyprctl keyword monitor <NAME>,<W>x<H>@<Hz>,<x>x<y>,<scale>` reusing the monitor's
      current position and scale — never `auto` for either (see design.md).
- [x] 2.8 After every apply, re-read `hyprctl monitors all -j` and re-render. The popover
      stays open.
- [x] 2.9 Add the "Reset to configured" action (`hyprctl reload`), followed by a re-read.
- [x] 2.10 Add the "Re-pack layout" action: re-apply every monitor with its current mode
      and scale but `auto` position, in left-to-right order of current x, then re-read.

## 3. Bar integration & styling

- [x] 3.1 `Bar.tsx`: import `Displays` and add `<Displays />` to the `end` box,
      immediately before `<SystemMonitor />`.
- [x] 3.2 `style.scss`: add `.Displays` StatusIcon styling and `.DisplaysPopover` with
      card, resolution-row, current/native marker, and warning-annotation rules,
      following the existing `.ScreenPicker` block.

## 4. Retire nwg-displays

- [x] 4.1 `modules/programs/desktop/de/hyprland.nix`: remove `nwg-displays` from the
      package list (line ~97). Leave `hyprviz` in place.
- [x] 4.2 `modules/USERS/pim/programs/hyprland/hypr/workspaces.conf`: replace the
      `# Generated by nwg-displays on … Do not edit manually.` header with an accurate
      one — the file is hand-maintained now.
- [x] 4.3 Grep for remaining `nwg-displays` references and update any that assert it
      owns monitor geometry.

## 5. Build & verify

- [x] 5.1 `nix build .#mipbar` compiles (stage the new files so the flake source
      sees them). Also added `checks.mipbar-displays`, which transpiles
      `widget/displayInfo.test.ts` with esbuild and runs it on node — 130 assertions,
      all passing via `nix build .#checks.<system>.mipbar-displays`.
- [x] 5.2 Card contents cross-checked against live `hyprctl monitors all -j`: model,
      13.9″ diagonal, 1920×1080, 60 Hz, scale 1.50 → 1280×720, 158/106 dpi, position
      2560,0, connector, port chip, active workspace. Verified by running the real
      helper pipeline over live output, and the bar renders with no stderr.
- [x] 5.3 `1680x1050` (divides evenly by scale 1.5): applied via the widget's own
      `monitorKeyword`; resolution took effect, scale stayed 1.50, position preserved.
- [x] 5.4 `1280x1024` (does not divide evenly): the row's pre-flight annotation said
      `⚠ scale → 1.60`, and Hyprland delivered exactly scale 1.6 — the prediction is
      truthful against the live compositor, not just in unit tests.
- [x] 5.5 Bar did not restart across 5.3/5.4 (same PID throughout) and emitted no
      stderr.
- [x] 5.6 "Reset to configured" (`hyprctl reload`): restored 1920×1080, scale 1.50,
      position 2560,0 exactly — byte-for-byte the baseline.
- [x] 5.7 Re-pack mechanism verified: applied in ascending-x order with `auto`
      position; mode and scale preserved, no monitor lost, and reset undid it. See
      5.11 for the part that needs a second display.
- [x] 5.8 No Hyprland configuration written: `monitors.conf` untouched in git and
      still a read-only `/nix/store` symlink at `~/.config/hypr/monitors.conf`.
- [x] 5.9 Workspace→monitor binding unaffected by the resolution changes.
- [x] 5.10 `nwg-displays` removed and the affected configurations still evaluate and
      build (gate 4 of `ship-change.sh`).

## 6. Deferred verification

These need a human at the screen or a second display, and are recorded rather than
claimed.

- [ ] 6.1 Open the popover and confirm the rendering visually — card layout, accent
      colours in both light and dark mode, the `⚠` annotations, and that the popover
      stays open across an apply. The data path is verified headlessly; only the
      pixels are unconfirmed.
- [ ] 6.2 With an external monitor attached: shrink it, confirm the positional gap
      appears, then "Re-pack layout" and confirm the gap closes and the cursor
      crosses cleanly. Only one display was attached during implementation.

## 7. Spec coordination

- [x] 7.1 Before archiving, check whether `fix-workspace-monitor-binding` has
      archived. It has NOT (7/11 tasks, still active), so this change archives first
      and its delta must be rebased onto the renamed
      `static-config-owns-no-dynamic-binding` requirement, dropping its nwg-displays
      sentences.
- [x] 7.2 `openspec validate add-mipbar-displays-menu` passes.
