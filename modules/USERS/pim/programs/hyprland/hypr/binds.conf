###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod SHIFT, RETURN, exec, $terminalDarkMode
bind = $mainMod SHIFT, B, exec, $browser
bind = $mainMod, Q, killactive,
bind = $mainMod SHIFT, M, exit,
bind = $mainMod SHIFT, F, exec, $fileManager
bind = $mainMod, W, togglefloating,
bind = $mainMod, S, exec, myhotkeys
bind = $mainMod, SPACE, exec, walker
bind = ALT, SPACE, exec, walker -m clipboard
bind = $mainMod, A, exec, nwg-panel # Quick settings
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, layoutmsg, togglesplit # dwindle

bind = Control_L SHIFT, right, movewindow, r
bind = Control_L SHIFT, left, movewindow, l

# Move active window to monitor left/right
bind = Control_L SHIFT, h, movewindow, mon:l
bind = Control_L SHIFT, l, movewindow, mon:r

#bind = $mainMod SHIFT, SPACE, exec, cliphist list | wofi --dmenu --pre-display-cmd "echo '%s' | cut -f 2" | cliphist decode | wl-copy

# Darkmode
bind = $mainMod, D, exec, gsettings set org.gnome.desktop.interface color-scheme "prefer-dark" && ~/.config/hypr/scripts/theme-wallpaper dark
bind = $mainMod SHIFT, D, exec, gsettings set org.gnome.desktop.interface color-scheme "prefer-light" && ~/.config/hypr/scripts/theme-wallpaper light
bind = $mainMod SHIFT, S, exec, $signal


# Lock screen.
#
# These resolve to hyprlock directly rather than to `loginctl lock-session`, so
# a manual lock still works if hypridle is not running. lock-session is a no-op
# with no error when no listener is registered, which would leave an unlocked
# screen behind and say nothing. hypridle's own lock_cmd is this same string,
# so both paths end up in one place without either depending on the other.
bind = $mainMod, L, exec, pidof hyprlock || hyprlock

# Suspend only. hypridle's before_sleep_cmd does the locking, and covers lid
# close and a hand-typed `systemctl suspend` at the same time. The old form was
# `hyprlock && systemctl suspend`, which suspended only AFTER the user had
# already come back and unlocked.
bind = $mainMod SHIFT, L, exec, systemctl suspend

# Switch to previous active workspace
bind = $mainMod, TAB, workspace, previous

# Screenshot with hyprshot (Ctrl+Shift+C)
bind = Control_L SHIFT, R, exec, hyprshot -m region
bind = Control_L SHIFT, W, exec, hyprshot -m window


# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, F, fullscreen

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
#bind = $mainMod, S, togglespecialworkspace, magic
#bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
bind = , mouse:274, exec, # disable middle click

# Resize windows with SHIFT + arrow keys
bind = $mainMod SHIFT, right, resizeactive, 30 0
bind = $mainMod SHIFT, left, resizeactive, -30 0
bind = $mainMod SHIFT, up, resizeactive, 0 -30
bind = $mainMod SHIFT, down, resizeactive, 0 30

#bindm = Control_L SHIFT, left, movewindow
#bindm = Control_L SHIFT, right, movewindow


## Noctalia Shell IPC keybindings
#bind = $mainMod, A, exec, noctalia-shell ipc launcher
#bind = $mainMod SHIFT, E, exec, noctalia-shell ipc session-menu
#bind = $mainMod, V, exec, noctalia-shell ipc volume

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

## Requires playerctl
#bindl = , XF86AudioNext, exec, playerctl next
#bindl = , XF86AudioPause, exec, playerctl play-pause
#bindl = , XF86AudioPlay, exec, playerctl play-pause
#bindl = , XF86AudioPrev, exec, playerctl previous


