# Shared theme colors — consumed by both tmux (via HM options) and neovim (via extraSpecialArgs).
# See modules/themes/default.nix and flake.nix nixvimModule.
{
  bg = {
    active = "#000000";
    inactive = "#292f32";
  };
}
